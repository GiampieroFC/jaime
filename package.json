{
  "name": "jaime",
  "module": "index.ts",
  "scripts": {
    "kill": "pkill -xc node",
    "clean": "rimraf ./dist",
    "build": "tsc",
    "dev": "pnpm clean && nodemon",
    "start": "pnpm clean && pnpm build && node ./dist/app.js"
  },
  "devDependencies": {
    "@types/node": "latest",
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@whiskeysockets/baileys": "^6.7.8",
    "qrcode-terminal": "^0.12.0",
    "zod": "^3.23.8"
  }
}